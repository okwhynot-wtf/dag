Diagonal Archive Geometry — symbolic law

═══════════════════════════════════════════════════════════════════════════════
0. Layers  (downward imports only)
═══════════════════════════════════════════════════════════════════════════════

  L2  Dictionary   physical instantiations  (K-cert + L-cert)
  L1  Ledger       S×E, registration, capacity, profile, address   (AG)
  L0  Spine        arena, Fund, seal, ladder, branch               (DM)

  naming tick  =  ladder step Lₖ ↦ Lₖ₊₁
  microtick    =  one U : S×E → S×E
  (Bridge.TickSimulation.tick_identification)

═══════════════════════════════════════════════════════════════════════════════
I. Spine  (DM arena)
═══════════════════════════════════════════════════════════════════════════════

  Obj:  (α, a:α→α, z)  with  a∘a = id          pointed unoriented acts
  Mor:  h∘a = b∘h,  h(z)=w

  a(z)=z                 REST       stagnation
  a(z)≠z ∧ a²(z)=z       2-CYCLE    survivor
  a²(z)≠z                ARROW      directed step
  a(x)=a(y) ∧ x≠y        ERASE      collapse

  Fund(a)  ⇔  a ≅ ¬      (Bool,¬,true) initial; unique up to label swap
  ∄ x. a(x)=x            void excluded
  a∘a=id  ⇒  ¬ERASE      involutivity (in-arena)
               ¬ARROW at Fund

  dodge(f) := λx. ¬(f x x)
  ∀f.  f misses dodge(f)               seal (never point-surjective)

  L₀ = Bool
  Lₖ₊₁ = Option(Lₖ),   none ↦ dodge(repₖ)
  namer ∉ im(some)                     forced +1 every tick
  Lₖ ↪ Lₖ₊₁,  Lₖ₊₁ ↪̸ Lₖ
  ∄ rung below L₀                      non-commencement
  □(ascent) ∧ ¬□(direction)            forced but free

═══════════════════════════════════════════════════════════════════════════════
II. Ledger  (AG over I-1 / I-2)
═══════════════════════════════════════════════════════════════════════════════

  U : S×E → S×E = (U_S, U_E)           joint microstep
  global Inj(U)                        I-1 transfer of spine losslessness
                                       (TwoBounce: U=σ∘φ on Fin n;
                                        canonicity_fails;
                                        channel_swap_is_reversal)
  merges live in U_S above base; compensated in U_E

  Inj(U) ∧ ∃merge(U)  ⟹  ∃record(U)    Registration
                                       (Bridge.Dil.registration)

  K ≥ 2 = |A|                          merge / blank alphabet
  Kmin = 2                             |Fund| = 2  (T-3)
  K > 2                                admissible, non-minimal

  E(T)  :=  L_T                        I-2 environment = ladder carrier
                                       (Dil: free initial; capacity law;
                                        K=2 schedule = caps;
                                        UF Hom∃ / iso / graded terminality;
                                        I-2 Fin alphabet-UF;
                                        address-uniform idx;
                                        tick_identification)
  |caps T| = 2^(T+2)                   predicateCount(T)
  committed prof = expand              prof(T)=T
  alive:  2^T ≤ 2^(T+2)                by arithmetic

  Inj · merge · mute · confined  ⇒  K^T ≤ |caps T|
  eternal ⇒ ¬flat(prof)
  bounce  ⇒ undirected                 (spine involutivity)
  bounded caps ∧ Inj ∧ merge  ⇒  ∃t StreamMute fails     T-10
  merge⇒record ∧ lift∧¬retract  ⇒  combinatorial 2nd law  T-11
  E1: structure grows; E2: caps = K^t, liveness ⇒ expand
  T-13: under NumberEqualsVolume, counted V = K^t;
        1 bit/tick at K=2
  E3 blocked by R-1 (ultrametric); E4 not derived (Friedmann+matter)
  T-14: tree Forman < 0; recombination raises Forman;
        live ⇒ ¬flat(prof)
  T-15: alive ⇔ records ≤ caps growth; saturation := equality;
        at saturation, record flux fixed by the capacity schedule
  T-16: flatness-debt balance — locality inputs; Forman unpaid
        flatness paid by faces; flux = caps at saturation; local stalks;
        continuum G_{μν}=8πT_{μν} not derived
  curvature precluded only on the 1-d line (trivial)
  arrow = (address, archive↑)

  StreamMute  ⇏  ProductFresh
  ProductFresh  ⇏  StreamMute

═══════════════════════════════════════════════════════════════════════════════
III. Dynamics classification & wave  (bridge)
═══════════════════════════════════════════════════════════════════════════════

  F : Bool⁴ → Bool                       general 2nd-order NN update
  toggle ∀(l,m,r)  ⇒  F = p ^^ G(l,m,r)  lossless forces the xor form
  ¬toggle  ⇒  3-ring collision           erasure exhibited pointwise
                                         (toggle_forces_xor_form;
                                          collision_of_not_toggling)
  K inter-site                           signature, declared in daylight
  parity ∧ pole-inv ∧ drawing  ⇒  K ∈ {xor, !xor}
                                         one flip orbit
                                         (coupling_uniqueness;
                                          survivors_one_orbit)
  flip K ↦ !K                            involution; class decided:
                                         packaging iff 4 ∣ n
                                         (coupling_flip_dichotomy, exp)
  survivor on ring = ringX               (survivor_is_wave_coupling; rfl)

  step X = swap ∘ shear X                definitional two-bounce (I-1)
  reverse = shear ∘ swap                 swap = reversal at field level
  Inj(step X)  ∀X ∀n                     lossless for every coupling
                                         (funext, flagged by name)
  order(shift k on Fin n) = n / gcd n k  gcd law, return + minimality
  rot k = reflAt k ∘ reflZero            dihedral closed form (rotFactor)
  twelve-carrier: fifth closes at 12     decidable instances of the law
  ring 4 ↦ 4;  Dirichlet 4 ↦ 10          boundary selects the spectrum
  ∀X ∀s ∃p ≤ 2^(2n): step_X^p(s) = s     return within capacity
                                         (choice-free pigeonhole;
                                          Classical.choice absent)

  Modules (bridge): DynamicsAmbient · WaveDynamics · PeriodSpectrum ·
                    Recurrence

═══════════════════════════════════════════════════════════════════════════════
IV. One ℤ/2  (T-7)
═══════════════════════════════════════════════════════════════════════════════

  pole swap  ≅  address ∈ {fwd,rev}  ≅  channel ∈ {φ,σ}

  one ℤ/2  ⟶≅  {pole, address, channel}     three isomorphic presentations
  stated once; three named projections
  coupling flip K ↦ !K : distinct involution (pole-inv fixes survivors);
  dichotomy (exp): 4 ∣ n ⇒ half-period mask conjugacy (packaging);
                   4 ∤ n ⇒ no intertwiner exists (content, "up to flip");
  every n: flipped run = plain run dressed per tick (period-4 schedule)

═══════════════════════════════════════════════════════════════════════════════
V. Dictionary  (I-4: K-certificate | L-certificate)
═══════════════════════════════════════════════════════════════════════════════

  K-cert (kernel):
    (a) involution swap   (b) observe(swap)=¬observe
    (c) Fixed ↔ locus (nonempty|empty)   (d) forced +1 no-go
    Quintom · BitFlip · Kramers · MetaProblem

  L-cert (ledger):
    (a) Inj(U)  (b) merge  (c) record  (d) caps/alive  (e) t_exh + T-10
    Page (finite information toy)

  Branch / measurement (T-12):
    □ascent ∧ ¬□direction · ≥2 children · histories diverge · no selection
    NoClone: Bell support ≠ product support; pairs with Lossless

  RE / quintom:
    swap ≅ ¬ ,  Fixed ≅ w=−1 ,  forced +1 ≅ second channel
    H = 0:  undamped · silent archive · Fund     oscillator triangle (T-5)
    H > 0:  contraction ⇒ merge ⇒ record         damping = registration (T-6)
    a(x)=x excluded; divide only at crossings / limit   (T-8)
    modeCount T = |Channel|^(T+2) = caps T · doubles at Kmin   (Growth)

  Page:
    Eternal mute merge; Registration+T-10 yields speech after exhaustion
    t_exh = Page time; before: mute admissible; after: archive speaks

  MetaProblem:
    self-opacity · outdated portrait · restless  ⇒  ineffability reports
    observers_forced = forced +1; experience not in scope
    SelfReference.deficit_count · lag_theorem · corpus_not_universal

═══════════════════════════════════════════════════════════════════════════════
VI. Identifications  (spine / ledger / dictionary)
    spine and ledger columns under tick_identification;
    dictionary column at K/L-certificate grade (I-4)
═══════════════════════════════════════════════════════════════════════════════

  naming tick             ≅  microtick (UF archives, up to record gauge;
                             Fund / period-2 exempt; swap necessity)
  Fund (live 2-cycle)     ≅  oscillator (Inj∧¬merge∧¬record)  ≅  H=0 quintom
  forgets count           ≅  archive silent                   ≅  envelope const
  involution / undirected ≅  undirected bounce                ≅  swap EOM
  pole / two_readings     ≅  address                          ≅  φ↔σ
  dodge / +1              ≅  blank adjunction                 ≅  second channel
  excluded fixed point    ≅  —                                ≅  w=−1 divide
  pred growth 2^(k+2)     ≅  c(t+1)=K·c(t), K=2               ≅  modeCount=|Channel|^(T+2)
                                                                   (=caps; doubles at Kmin)
  erasure priced out      ≅  merge ⇒ record                   ≅  H → expansion
  undamped ⇒ no naming ticks; arrow iff registration
                             (time_dissipation_one_property)

═══════════════════════════════════════════════════════════════════════════════
VII. Compression
═══════════════════════════════════════════════════════════════════════════════

  (Fund ≅ ¬ ∧ seal ∧ ladder +1)
    +  (Inj + merge ⇒ record) · (mute ⇒ K^T ≤ cap) · (eternal ⇒ nonflat)
    +  (bounded ⇒ archive speaks)                      T-10
    +  (merge⇒register ∧ ¬retract)                           T-11
    +  (□ascent ∧ ¬□direction)                         T-12
    +  (liveness ⇒ caps↑ · number=volume ⇒ V=K^t)      T-13
    +  (tree Forman < 0 · recomb ↑ Forman · ¬flat)     T-14
    +  (saturation: ledger balance fixes flux)         T-15
    +  (flatness debt · Forman balance · stalks)       T-16
    +  (E = L_• ,  |caps T| = 2^(T+2) ,  Kmin = 2)
    +  (modeCount = |Channel|^(T+2) = caps · doubles at Kmin)
    +  (arrow = address = pole = channel    :  one ℤ/2)
    +  (lossless ⇒ xor form · K ∈ {xor,!xor} · step = swap∘shear
        · ord = n/gcd · return ≤ 2^(2n) · tail erases at the edge)
    +  (H=0 ⇒ oscillator triangle;  H>0 ⇒ dissipation registers)
    +  (undamped ⇒ no naming ticks; arrow iff registration)
    +  (interior triad ⇒ ineffability reports)

═══════════════════════════════════════════════════════════════════════════════
VIII. Scope
═══════════════════════════════════════════════════════════════════════════════

  Not derived here:
          continuous one-parameter boost / Unruh temperature (O-2);
          continuum Lorentzian limit (O-4);
          Mathlib sheaf/site locality (O-3 combinatorial stalks only);
          ultrametric ≠ Archimedean space (R-1);
          Standard Model inventory from Bool (O-5);
          H₀ / cosmology numbers from Bool;
          continuum spacetime, Einstein field equations, QFT;
          continuum Boltzmann / Gibbs;
          Born rule from multiplicity alone
            (born_from_multiplicity_nogo);
          host-logic Gödel arithmetisation of the corpus;
          experience / qualia;
          dispersion relations / continuum frequencies;
          closed-form wave period law in general n
            (needs F₂[x]/(xⁿ−1); not in corpus)

  Gauge demarcation:
               content = exported statement mentions the choice;
               packaging = quotiented before downstream consumption
  Gauge inventory:
               label/pole swap @ Fund     — content  (T-7)
               address fwd/rev            — content  (T-7)
               channel swap               — content  (channel_swap_is_reversal)
               record gauge per tick      — packaging (Dil / T-2)
               factorisation gauge        — packaging (I-1)
               coupling flip K ↦ !K         packaging iff 4 ∣ n, else
                                            content (coupling_flip_dichotomy,
                                            exp; inventory complete)

  Discharged in corpus (selected):
               ¬flat(prof); Kmin=2; |caps T|=2^(T+2); one ℤ/2;
               recordLabel∶Bool↪E; tick_identification;
               straighten; registers_admits_namer;
               born_from_multiplicity_nogo;
               canonicity_fails; channel_swap_is_reversal;
               coupling_flip_dichotomy;
               T-10…T-16; E1; E2;
               corpus_not_universal; seal_self; deficit; lag;
               ω≠rung; observers_forced

  Modules:
  spine:   Canon · Orbit · Diagonal · Ladder · Density · NonCommencement ·
           Branch · Interior · OmegaDuration · SelfReference
  ledger:  Geom.Registration · Profile · Gradient · Obs.Recovery · Obs.Dimension
  bridge:  Alphabet · Capacity · OneZ2 · RegistrationSpine · Environment ·
           TickSimulation · RegistrationFactor · ArchiveMustSpeak · ArchiveMonotone ·
           BranchNondeterminism · LadderGrowth · Forman · Saturation · FlatnessDebt ·
           FiberFlux · FluxPattern · LedgerPatchGlue · RecombinationBudget ·
           PhaseEToys · Dil · PageShape · BranchMeasure · CutShift · TwoBounce ·
           DynamicsAmbient · WaveDynamics · PeriodSpectrum · Recurrence
  dict:    Certificate (K) · LCertificate · Quintom.* (incl. Growth) · BitFlip ·
           Kramers · Page · QEC · NoClone · MetaProblem

═══════════════════════════════════════════════════════════════════════════════
IX. Experimental annex  (quarantined; no main-line module imports it)
═══════════════════════════════════════════════════════════════════════════════

  boundary = Nat → Bool                  rungs embed; depth-(k+1)
                                         cylinders separate rung k;
                                         flip Live ⇒ seal at boundary
                                         carrier; tail erases (in-arena
                                         ¬erase fails at the boundary);
                                         cons/tail carries the +1 step
                                         (presented in the paper's
                                          boundary section; module
                                          stays quarantined, all
                                          declarations axiom-free)
  sym order(ring wave) = n / gcd n (n-1) = n
                                         gcd law computes the
                                         symmetry order; spectrum
                                         constant on rotation orbits
  flip gauge dichotomy (CouplingGauge)   4 ∣ n: half-period mask
                                         conjugacy (packaging;
                                         funext, recorded);
                                         4 ∤ n: no intertwiner
                                         (content, "up to flip");
                                         per-tick period-4 dressing
                                         at every n; flipped pulse
                                         periods 4,12,4,20 at n=2..5
  periods: ring 2..6 ↦ 2,6,4,10,6        kernel-decided; laws
           Dirichlet 3..5 ↦ 8,10,12      (even↦n, odd↦2n, 2n+2)
                                         conjectural beyond (needs
                                         F₂[x]/(xⁿ−1); not in corpus)
  concrete exhibits                      Fin 3 / Fin 5 cycles;
                                         fifth_generates; twelve-carrier
                                         instances of the gcd law;
                                         waveFactor packaged witness
                                         (funext, recorded)

  Not derived: continuum, real frequencies, dispersion over ℝ,
               closed-form period law in general n
  Footprints:  propext / Quot.sound only; Classical.choice appears
               nowhere in the repository's packages. Audited per
               declaration in formal/AXIOMS.md
  Modules:     Overtones · Frequencies · WaveEquation ·
               CantorBoundary · SpectrumSymmetry · CouplingGauge

═══════════════════════════════════════════════════════════════════════════════
  Fund ≅ ¬  ·  a∘a=id ⇒ ¬erase  ·  merge ⇒ record  ·  |caps T| = 2^(T+2)
  ·  naming ≅ μtick / gauge  ·  (I-1 · swap=reversal · T-7)
  ·  lossless ⇒ xor · K ∈ {xor,!xor} · return ≤ 2^(2n)
═══════════════════════════════════════════════════════════════════════════════
