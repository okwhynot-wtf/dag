# O-2 decision note — boost / Unruh temperature

**Status:** open decision. One genuine attempt recommended before fencing.

## Two respectable positions

### Attack (KMS / modular route)

The period-2 KMS toy (`Period2KMS.period2_kms_caricature`) is the natural
seed: Unruh temperature is modular in origin. A corpus analogue of a boost
is plausibly a **shift of the cut** between system and archive at saturation,
with:

- KMS period supplying combinatorial temperature;
- area-as-caps (`areaBits`) supplying the horizon side.

If a discrete modular flow can be extracted from cut position, O-2 opens.
Seed lemmas already in hand: period-2 under ℤ/2, constant `T_c`, saturation
EoS (T-15), seal-per-reading horizon (T-16).

### Refusal (feature, not bug)

With one ℤ/2 and nothing continuous, the absence of boosts may be a
**prediction**: Lorentz structure emerges only in a continuum limit the
corpus refuses to take. Honesty then requires stating that, and noting
discreteness-scale Lorentz violation is empirically constrained territory.
O-2 stays structural; deepest Einstein gap remains correctly labelled.

## Recommended posture

Spend a focused pass on the KMS/cut-shift route before choosing. Do not
reclassify O-2 as tractable without a concrete modular-flow fragment; do not
fence it as permanent refusal without that attempt.
