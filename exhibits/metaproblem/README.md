# Meta-problem exhibit notes

Companion to `formal/dictionary/MetaProblem.lean`.

## Claimed

Chalmers's **meta-problem** explanandum — systems report ineffability,
incompleteness, and an unstable self — is theorem-shaped behaviour of any
live self-representing ladder system:

| Report | Theorem |
|---|---|
| Opaque to itself | `Interior.self_opacity` / `SelfReference.seal_self` |
| Self-model stale | `Interior.outdated_portrait` / `SelfReference.lag_theorem` |
| Self won't sit still | `Interior.self_modeling_forces_restless` / `SelfReference.lag_recedes` |
| Forced thin observer | `Observer.observers_forced` |
| Corpus incompleteness | `SelfReference.corpus_not_universal` |
| Quantitative triad | `Dictionary.MetaProblem.meta_quantitative` |

Junction: corpus representing corpus (Lawvere). Host-logic Gödel and
arithmetised syntax routes refused — see `formal/spine/SelfReference.lean`.

## Refused

The **hard problem** (qualia / "what it is like"). Spine scope already
commits: Interior entails nothing about experience. Neutral-monist reading
only.
