# Quintom / DESI — null-or-shape note

Recent DESI analyses favour evolving dark energy with possible phantom
crossing. Within DAG:

- Kernel side (Lean): H=0 oscillator triangle; H>0 damping = registration
  skeleton; fixed-point discipline at w = −1.
- Exhibit side: ODE numerics in `integrate.py` (envelope decay when H>0).

**Constraint check.** The framework does **not** currently derive a
forecast for `w(z)` near the divide, nor a DESI-compatible crossing
probability. Action parameters (m, V₀, amplitudes) remain inputs.

**Null statement (claim hygiene).** Until a theorem constrains the shape
of `w(z)` from spine/ledger structure alone, the quintom entry stays a
**certificate** (existence of a kernel realisation), not a **prediction**.
Observational liveness of the model class is noted; DESI is not a DAG
forecast.
